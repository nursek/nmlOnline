# ====================================================
# Profil DEV uniquement — NE PAS utiliser en production
# Activer avec : --spring.profiles.active=dev
# ====================================================

# Console H2 activée en dev uniquement
spring.h2.console.enabled=true

app.cookie.secure=false

# Secrets dev-only (valeurs non secrètes, usage local uniquement)
JWT_SECRET=dev-only-secret-key-minimum-32-chars-do-not-use-in-prod
JWT_PEPPER=dev-only-pepper-secret-minimum-32-chars-do-not-use-in-prod
